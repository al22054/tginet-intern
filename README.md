## GitHub運用マニュアル

### 1. ブランチの使い分け

- `main`
  - 公開用ブランチ
  - 直接Pushは行わない
  - Pull Requestでレビュー・承認後にMergeする

- `develop`
  - 修正内容をまとめるためのブランチ
  - mainへ反映する前の確認用として使用する

- 作業ブランチ
  - 各メンバーが個別に修正する際に使用する
  - `feature/〇〇`、`fix/〇〇` などの名前を付ける

### 2. Push・Pull Requestの方法

1. developブランチから作業ブランチを作成する
2. ファイルを修正する
3. 変更内容をCommitする
4. 作業ブランチをGitHubへPushする
5. GitHub上でPull Requestを作成する
6. レビュー担当者を指定する
7. 承認後にMergeする

基本的な流れ：

`作業ブランチ → develop → main`

### 3. レビューの方法

レビュー担当者はPull Requestの変更内容を確認する。

確認項目：

- 内容に誤りがないか
- 不要な変更が含まれていないか
- 表示崩れがないか
- 公開して問題ない内容か

問題がない場合はApproveする。

修正が必要な場合はコメントを記載し、修正後に再度レビューを行う。

### 4. レビュー担当者

Pull Request作成時に、あらかじめ決められたレビュー担当者を指定する。

現在のレビュー担当者：

- 担当者1：
- 担当者2：

### 5. 承認に必要な人数

mainブランチへMergeする場合は、
**1名以上のレビュー担当者による承認を必須とする。**

### 6. mainへMergeする条件

以下の条件をすべて満たした場合のみmainへMergeする。

- Pull Requestが作成されている
- レビュー担当者による確認が完了している
- 必要な人数のApproveを取得している
- 修正依頼が残っていない
- GitHub Actionsでエラーが発生していない

### 7. 担当者変更時の対応

レビュー担当者が変更された場合は、以下の対応を行う。

1. 新しいレビュー担当者を決定する
2. 本READMEの「レビュー担当者」を更新する
3. GitHub上のレビュー担当者設定を変更する
4. Branch protectionまたはRulesetの設定を確認する
5. 必要に応じてCODEOWNERSを更新する
6. 新しい担当者へ運用方法を共有する

担当者が変更された場合でも同じ運用を継続できるよう、設定変更とあわせてREADMEも更新する。